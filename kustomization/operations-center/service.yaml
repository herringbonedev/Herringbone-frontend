apiVersion: v1
kind: Service
metadata:
  name: operations-center
  namespace: herringbone
spec:
  selector:
    app: operations-center
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
